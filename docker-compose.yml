
version: '3'
services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: shellton/nabad:latest
    restart: always
    ports:
      - "3126:3126"
    networks:
      - app_network
    environment:
      - NODE_ENV=production
networks:
  app_network:
    driver: bridge
